= Event Service Contributing Guide
include::docs/asciidoc/_attributes.adoc[]
ifdef::env-idea[]
:toc: preamble
endif::[]
ifeval::[{env}!=idea]
:toc: left
endif::[]
:doctype: book

This guide is for engineers who are working on {project-display-name}

toc::[]

PRs are always welcome!

For details on the V4 Pact specification, refer to https://github.com/pact-foundation/pact-specification/tree/version-4

For details on the Pact plugin framework, refer to https://github.com/pact-foundation/pact-plugins

== Raising defects

Before raising an issue, make sure you have checked the open and closed issues to see if an answer is provided there.
There may also be an answer to your question on https://stackoverflow.com/questions/tagged/pact[stackoverflow].

Please provide the following information with your issue to enable us to respond as quickly as possible.

. The relevant versions of the packages you are using (plugin and Pact versions).
. The steps to recreate your issue.
. An executable code example where possible.

=== New features / changes

. Fork it
. Create your feature branch (git checkout -b my-new-feature)
. Commit your changes (git commit -am 'feat: Add some feature')
. Push to the branch (git push origin my-new-feature)
. Create new Pull Request

==== Commit messages

We follow the https://github.com/bcoe/conventional-changelog-standard/blob/master/convention.md[Conventional Changelog]
message conventions. Please ensure you follow the guidelines.

Rust changes use `feat(rust):` / `fix(rust):`. Breaking changes append `!`.

=== Building the plugin

The plugin is written in Rust. Building it needs the toolchain pinned in `modules/plugin-rs/rust-toolchain.toml`.

[source,shell]
----
cd modules/plugin-rs
cargo build --release
cd ../..
bash scripts/pluginLocalInstall.sh
----

`scripts/pluginLocalInstall.sh` copies the binary and the plugin manifest into the Pact plugin directory. See the installation instructions on {doc-url}.

=== Running the tests

.Unit tests
[source,shell]
----
cd modules/plugin-rs
cargo test --locked
----

Golden and schema fixtures under `modules/plugin-rs/tests/fixtures` change only in a commit whose message states why the behavior changed.

.Consumer tests
[source,shell]
----
sbt consumer/test
----

.Run Pact broker
[source,shell]
----
docker compose -f docker-compose.yml up -d
----

.Upload Consumer Pact
[source,shell]
----
./scripts/pact-publish.sh
----

.Provider tests
[source,shell]
----
sbt provider/test
----

.Stop Pact broker
[source,shell]
----
docker compose -f docker-compose.yml down
----

=== Releasing

Releases are cut by running the "Prepare release" workflow. It computes the next version from Conventional Commit subjects since the last tag and opens a `chore: bump version -> X.Y.Z` pull request.
Squash- or rebase-merge that pull request: a merge commit does not match the release trigger, so nothing is tagged or published. The merge tags the commit and publishes the release.
To release a version outside the computed bump, run "Prepare release" with `new_tag` set to `vX.Y.Z`; the run needs approval in the `release` environment.
If a release run fails after the tag is pushed, re-run the failed workflow run: tagging is idempotent, and a version whose release already exists is refused.
If the post-publish smoke test fails, fix on `main` and cut a patch release; never delete or move a published tag or release.
After the smoke test passes, the run opens a pull request against https://github.com/pact-foundation/pact-plugins[pact-foundation/pact-plugins] that adds the version to its plugin index; until that merges, `pact-plugin-cli install avro` does not see the release.
The job needs the `PACT_PLUGINS_PR_TOKEN` secret and never fails the release run. Without the token, or to retry, run `GH_TOKEN=<token> bash scripts/index-pr.sh vX.Y.Z`.

include::docs/README.adoc[]
