= Logging

The plugin writes logs to standard error. It does not write a log file.

The default log level is `info`. Set the `RUST_LOG` environment variable in the environment that launches the plugin process (for example `RUST_LOG=debug`) to change it.
